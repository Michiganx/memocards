<li id="deck-<%= deck.id %>">
  <span class="user"><%= link_to deck.user.name, deck.user %></span>
  <span class="name"><%= deck.name %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(deck.created_at) %> ago.
  </span>
</li>